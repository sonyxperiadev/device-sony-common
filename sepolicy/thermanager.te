type thermanager, domain;
type thermanager_exec, exec_type, file_type;

# Started by init
init_daemon_domain(thermanager)

allow thermanager self:capability dac_override;

allow thermanager sysfs_thermal:file rw_file_perms;

allow thermanager sysfs_performance:dir search;
allow thermanager sysfs_performance:file rw_file_perms;
