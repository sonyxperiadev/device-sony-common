type timekeep_app, domain;

app_domain(timekeep_app)

allow timekeep_app timekeep_app_data_file:dir create_dir_perms;

set_prop(timekeep_app, timekeep_prop)

allow timekeep_app sysfs_timekeep:file r_file_perms;
